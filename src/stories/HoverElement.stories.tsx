import HoverElement from "../components/common/HoverElement";
import React from "react";

export default {
  title: "HoverElement",
  component: HoverElement,
};

const Template = (args: any) => <HoverElement {...args} />;

export const Default: any = Template.bind({});
